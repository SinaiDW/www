-- 
-- Editor SQL for DB table projects
-- Created by http://editor.datatables.net/generator
-- 

CREATE TABLE IF NOT EXISTS `projects` (
	`id` int(10) NOT NULL auto_increment,
	`name` varchar(255),
	PRIMARY KEY( `id` )
);